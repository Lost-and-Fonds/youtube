# Stashd YouTube plugin

Planned YouTube Input package for Stashd. The production-native provider is not
implemented yet; M11 owns that work. The former Wasm implementation remains
historical reference material in `Lost-and-Fonds/stashd`.

When M11 starts, this repository will add the native package manifest,
entrypoint, provider implementation, fixtures, and SDK compatibility tests.
